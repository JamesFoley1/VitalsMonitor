# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VitalsMonitor' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VitalsMonitor

pod 'Alamofire', '~> 5.0.0-rc.2'

  target 'VitalsMonitorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VitalsMonitorUITests' do
    # Pods for testing
  end

end

target 'VitalsMonitor WatchKit App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VitalsMonitor WatchKit App

end

target 'VitalsMonitor WatchKit Extension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VitalsMonitor WatchKit Extension

end
